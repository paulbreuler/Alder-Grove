[package]
name = "grove-sync"
version = "0.1.0"
edition = "2024"

[dependencies]
grove-domain = { path = "../grove-domain" }
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v7", "serde"] }
