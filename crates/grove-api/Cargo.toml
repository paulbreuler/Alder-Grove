[package]
name = "grove-api"
version = "0.1.0"
edition = "2024"

[dependencies]
grove-domain = { path = "../grove-domain" }
grove-sync = { path = "../grove-sync" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v7", "serde"] }
