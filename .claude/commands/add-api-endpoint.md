# /add-api-endpoint

Run the canonical add-api-endpoint workflow from `.claude/skills/add-api-endpoint/SKILL.md`.

## Workflow

1. Read `.claude/skills/add-api-endpoint/SKILL.md`.
2. Follow that workflow and output format exactly.
3. Resolve any relative paths from `.claude/skills/add-api-endpoint/`.
