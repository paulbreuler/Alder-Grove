# /pr

Run the canonical pull request workflow from `.claude/skills/pr/SKILL.md`.

## Workflow

1. Read `.claude/skills/pr/SKILL.md`.
2. Follow that workflow and output format exactly.
3. Resolve any relative paths from `.claude/skills/pr/`.
