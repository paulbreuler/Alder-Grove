# /add-extension

Run the canonical add-extension workflow from `.claude/skills/add-extension/SKILL.md`.

## Workflow

1. Read `.claude/skills/add-extension/SKILL.md`.
2. Follow that workflow and output format exactly.
3. Resolve any relative paths from `.claude/skills/add-extension/`.
