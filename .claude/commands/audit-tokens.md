# /audit-tokens

Run the canonical audit-tokens workflow from `.claude/skills/audit-tokens/SKILL.md`.

## Workflow

1. Read `.claude/skills/audit-tokens/SKILL.md`.
2. Follow that workflow and output format exactly.
3. Resolve any relative paths from `.claude/skills/audit-tokens/`.
