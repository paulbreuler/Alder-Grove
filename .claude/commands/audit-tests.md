# /audit-tests

Run the canonical audit-tests workflow from `.claude/skills/audit-tests/SKILL.md`.

## Workflow

1. Read `.claude/skills/audit-tests/SKILL.md`.
2. Follow that workflow and output format exactly.
3. Resolve any relative paths from `.claude/skills/audit-tests/`.
