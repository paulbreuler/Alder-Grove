# /add-feature

Run the canonical add-feature workflow from `.claude/skills/add-feature/SKILL.md`.

## Workflow

1. Read `.claude/skills/add-feature/SKILL.md`.
2. Follow that workflow and output format exactly.
3. Resolve any relative paths from `.claude/skills/add-feature/`.
