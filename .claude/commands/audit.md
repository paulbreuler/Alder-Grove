# /audit

Run the canonical audit workflow from `.claude/skills/audit/SKILL.md`.

## Workflow

1. Read `.claude/skills/audit/SKILL.md`.
2. Follow that workflow and output format exactly.
3. Resolve any relative paths from `.claude/skills/audit/`.
