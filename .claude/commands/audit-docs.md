# /audit-docs

Run the canonical audit-docs workflow from `.claude/skills/audit-docs/SKILL.md`.

## Workflow

1. Read `.claude/skills/audit-docs/SKILL.md`.
2. Follow that workflow and output format exactly.
3. Resolve any relative paths from `.claude/skills/audit-docs/`.
